export type InventoryWithdrawRefused = {
  id: string;
  name: string;
  description: string;
  category: string;
  local: string;
  refusedAt: string;
  photoFilename: string;
  userId: string;
  isVisible: boolean;
};
